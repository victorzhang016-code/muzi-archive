# 仅 `npm run dev:prod`（vite --mode prod）加载。
# 让本地 dev 显式连【生产】Firestore —— 仅在确需对着生产库排查时用，会消耗额度。
# 注意：mode 名是 "prod" 而非 "production"，所以 import.meta.env.DEV 仍为 true，
# 不会被当成生产构建；生产构建（vite build, mode=production）不会加载本文件。
VITE_ALLOW_PROD=true
