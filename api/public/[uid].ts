import type { VercelRequest, VercelResponse } from '@vercel/node';
import { supabaseRest } from '../_lib/supabase';
export default async function handler(req: VercelRequest,res: VercelResponse){const uid=String(req.query.uid||'');const limit=req.query.limit?Number(req.query.limit):null;if(!uid)return res.status(400).json({error:'uid required'});const r=await supabaseRest('rpc/get_public_wardrobe',{method:'POST',body:JSON.stringify({p_public_id:uid,p_limit:limit})});if(!r.ok)return res.status(503).json({error:'busy'});const data=await r.json();if(!data)return res.status(403).json({wardrobePublic:false});res.setHeader('Cache-Control','public, s-maxage=300, stale-while-revalidate=3600');return res.status(200).json(data);}
