import type { VercelRequest,VercelResponse } from '@vercel/node';
import { supabaseRest } from '../../_lib/supabase';
export default async function handler(req:VercelRequest,res:VercelResponse){const uid=String(req.query.uid||''),id=String(req.query.id||'');const r=await supabaseRest('rpc/get_public_item',{method:'POST',body:JSON.stringify({p_public_id:uid,p_id:id})});if(!r.ok)return res.status(503).json({error:'busy'});const item=await r.json();if(!item)return res.status(403).json({shared:false});res.setHeader('Cache-Control','public, s-maxage=300, stale-while-revalidate=3600');return res.status(200).json({item});}
