import type { VercelRequest,VercelResponse } from '@vercel/node';
const base=process.env.SUPABASE_URL||process.env.VITE_SUPABASE_URL,key=process.env.SUPABASE_PUBLISHABLE_KEY||process.env.VITE_SUPABASE_PUBLISHABLE_KEY;
export default async function handler(req:VercelRequest,res:VercelResponse){const uid=String(req.query.uid||''),id=String(req.query.id||''),isMatch=req.query.c==='match';if(!base||!key)return res.status(500).end();const rpc=isMatch?'get_public_match':'get_public_item';const r=await fetch(`${base}/rest/v1/rpc/${rpc}`,{method:'POST',headers:{apikey:key,authorization:`Bearer ${key}`,'content-type':'application/json'},body:JSON.stringify({p_public_id:uid,p_id:id})});if(!r.ok)return res.status(503).end();const data=await r.json();const url=isMatch?data?.match?.photoBase64:data?.imageUrl;if(!url||typeof url!=='string')return res.status(404).end();res.setHeader('Cache-Control','public, s-maxage=86400, stale-while-revalidate=604800');return res.redirect(307,url);}
