import type { VercelRequest,VercelResponse } from '@vercel/node';
import { supabaseRest } from '../../_lib/supabase';
export default async function handler(req:VercelRequest,res:VercelResponse){const uid=String(req.query.uid||''),id=String(req.query.id||''),isMatch=req.query.c==='match';const rpc=isMatch?'get_public_match':'get_public_item';const r=await supabaseRest(`rpc/${rpc}`,{method:'POST',body:JSON.stringify({p_public_id:uid,p_id:id})});if(!r.ok)return res.status(503).end();const data=await r.json();const url=isMatch?data?.match?.photoBase64:data?.imageUrl;if(!url||typeof url!=='string')return res.status(404).end();res.setHeader('Cache-Control','public, s-maxage=86400, stale-while-revalidate=604800');return res.redirect(307,url);}
