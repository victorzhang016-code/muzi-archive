import { useState } from 'react';
import { supabase } from '../lib/supabase';
export function ResetPassword(){const[password,setPassword]=useState('');const[note,setNote]=useState<string|null>(null);const save=async()=>{if(password.length<8){setNote('密码至少 8 位');return;}const{error}=await supabase!.auth.updateUser({password});setNote(error?error.message:'密码已更新，可以返回首页登录。');};return <main className="min-h-screen bg-kraft flex items-center justify-center px-6"><section className="w-full max-w-sm space-y-4"><h1 className="text-xl font-bold">设置新密码</h1><input type="password" value={password} onChange={(e)=>setPassword(e.target.value)} placeholder="至少 8 位" className="w-full rounded-full border px-4 py-3"/><button onClick={save} className="w-full rounded-full bg-ink text-kraft py-3">保存密码</button>{note&&<p className="text-sm">{note}</p>}</section></main>}
