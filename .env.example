# 作者账号 uid，用于登录页卡墙 / 新用户示例卡 / /author 公开预览。
# VITE_ 前缀 = 构建期变量，改了要重新构建/部署。
VITE_AUTHOR_UID="your-firebase-uid"

# 注：KIMI_API_KEY 是服务端变量（无 VITE_ 前缀，只配在 Vercel 环境变量里，不放这里）。
# 注：本地连模拟器用 .env.emulator + `npm run dev:emu`，不需要在 .env 里配。
